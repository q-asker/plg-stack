# =============================================================
# 원격 노드 (broker-1, broker-2, broker-3, springboot) 공통 환경변수
# 각 노드에 복사 후 값 수정
# =============================================================

# Loki 엔드포인트 (#4 Monitoring 인스턴스)
LOKI_URL=http://monitoring.example.com:3100/loki/api/v1/push

# Prometheus remote_write 엔드포인트 (#4 Monitoring 인스턴스)
PROMETHEUS_URL=http://monitoring.example.com:9090/api/v1/write
